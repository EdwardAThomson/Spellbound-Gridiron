---
harness:
  generated_by: plimsoll/0.1
  run_id: r_10968a13b003
  generated_at: 2026-08-10T02:00:58Z
  regenerable: false
---

mode: closed
items:
  - id: i_50727e35c8f6
    statement: "Task 1: give Blizzard a -1 Move penalty for all players (in addition to +2 pass difficulty) in the pure rules layer, unit-test it, and make GAME_RULES, RuleBookModal, README (and CLAUDE.md) all describe Blizzard as -1 Move and +2 pass difficulty."
    kind: build
    owner: agent
    bears_on: ""
    deps: []
    scope: ["services/rules.ts", "services/rules.test.ts", "utils/contextSerializer.ts", "components/", "README.md", "CLAUDE.md"]
    verify:
      - command: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && grep -iqE 'blizzard' services/rules.ts && grep -iqE 'blizzard.*move|move.*blizzard' services/rules.test.ts && npm test"
        negative_control: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && ! grep -iqE 'blizzard.*move|move.*blizzard' services/rules.test.ts"
        assertion_regex: "([0-9]+) passed"
      - command: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && grep -iq blizzard README.md && grep -iq -- '-1 move' README.md && grep -iq -- '-1 move' utils/contextSerializer.ts && grep -Riq -- '-1 move' components/"
        negative_control: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && ! grep -iq -- '-1 move' README.md"
  - id: i_44bfa1390456
    statement: "Task 2: add an XP-and-progression system (documented XP awards from tackles/passes/touchdowns/spell casts, level thresholds, role-capped stat bumps) as rng-injected pure logic in services/rules.ts with unit tests; put XP/level in GameState and survive the versioned save/load round trip (bump SAVE_VERSION, migrate or gracefully reject old saves) with unit tests; show XP/level on the left-panel unit card; add an e2e spec asserting XP visibly accrues after a scoring play."
    kind: build
    owner: agent
    bears_on: ""
    deps: ["i_50727e35c8f6"]
    dep_reasons:
      - depends_on: "i_50727e35c8f6"
        reason: "Edits the same files (services/rules.ts, services/rules.test.ts, utils/contextSerializer.ts, README.md, CLAUDE.md); its diff must apply after Task 1's Blizzard edits to those files."
    scope: ["services/rules.ts", "services/rules.test.ts", "services/saveGame.ts", "services/saveGame.test.ts", "types.ts", "App.tsx", "utils/contextSerializer.ts", "components/", "README.md", "CLAUDE.md", "e2e/"]
    verify:
      - command: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && grep -iqE 'xp|experience' services/rules.ts && grep -iqE 'level|xp' services/rules.test.ts && npm test"
        negative_control: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && ! grep -iqE 'xp|experience' services/rules.ts"
        assertion_regex: "([0-9]+) passed"
      - command: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && grep -RiqE 'xp|level' services/*.test.ts && grep -RiqE 'load|save|migrat|version' services/*.test.ts && npm test"
        negative_control: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && ! grep -RiqE 'xp|level' services/*.test.ts"
        assertion_regex: "([0-9]+) passed"
      - command: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && grep -RiqE 'xp|level' e2e/ && npm run test:e2e"
        negative_control: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && ! grep -RiqE 'xp|level' e2e/"
        assertion_regex: "([0-9]+) passed"
  - id: i_5f57dbdcef8b
    statement: "Task 3: add named-slot persistent rosters that carry teams (players with XP/levels/bumps) across matches via localStorage alongside the existing save system, with a post-game rematch flow reusing persisted rosters and graceful degradation to fresh teams on corrupt/missing data; cover roster round-trip fidelity with unit tests and the rematch flow with an e2e spec; update all rule docs, then confirm the full CI gate is green."
    kind: build
    owner: agent
    bears_on: ""
    deps: ["i_44bfa1390456"]
    dep_reasons:
      - depends_on: "i_44bfa1390456"
        reason: "Rosters carry the XP/levels/stat-bumps defined by Task 2, and it edits the same files (services/rules.ts, services/saveGame.ts, types.ts, App.tsx, e2e/); its diff must apply after Task 2's commit."
    scope: ["services/rules.ts", "services/rules.test.ts", "services/saveGame.ts", "services/saveGame.test.ts", "types.ts", "App.tsx", "utils/contextSerializer.ts", "components/", "README.md", "CLAUDE.md", "e2e/"]
    verify:
      - command: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && grep -RiqE 'roster' services/rules.ts && grep -RiqE 'roster|slot' services/*.test.ts && npm test"
        negative_control: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && ! grep -RiqE 'roster' services/rules.ts"
        assertion_regex: "([0-9]+) passed"
      - command: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && grep -Riq 'rematch' e2e/ && npm run test:e2e"
        negative_control: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && ! grep -Riq 'rematch' e2e/"
        assertion_regex: "([0-9]+) passed"
      - command: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && grep -iqE 'xp|experience' services/rules.ts && grep -Riq 'rematch' e2e/ && grep -iq -- '-1 move' utils/contextSerializer.ts && npm run check"
        negative_control: "cd /home/edward/Projects/octonion-software/Spellbound_Gridiron && ! grep -iq -- '-1 move' utils/contextSerializer.ts"
        assertion_regex: "([0-9]+) passed"
